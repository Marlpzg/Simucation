package main;

import java.awt.*;

public class Values {

    private static Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();
    public static double WIDTH = screenSize.getWidth()/1.8;
    public static double HEIGHT = screenSize.getHeight()/1.6;

}
